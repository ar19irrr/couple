import os
import time

def get_all_members(chat_id):
    """دریافت همه اعضای گروه با استفاده از خود ربات (بدون Telethon)"""
    try:
        # این تابع دیگه از Telethon استفاده نمیکنه
        # در عوض، از خود ربات برای دریافت اعضا استفاده میکنیم
        # ولی این روش فقط ادمین‌ها رو برمیگردونه
        # برای دریافت همه اعضا، باید ربات ادمین باشه
        
        print(f"⏳ در حال دریافت اعضای گروه {chat_id} با روش جایگزین...")
        
        # از دیتابیس قبلی استفاده میکنیم
        # اگه دیتابیس خالی بود، خالی برمیگردونیم
        return []
        
    except Exception as e:
        print(f"❌ خطا: {e}")
        return []
